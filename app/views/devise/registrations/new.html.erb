<div class="col-xs-2">
  <div class="submenu list-group">
    <%= link_to '加入我們', new_member_registration_path, class: 'list-group-item active' %>
    <%= link_to '會員登入', new_member_session_path, class: 'list-group-item' %>
    <%= link_to '會員名單', members_path, class: 'list-group-item' %>
    <%= link_to '會員討論區', members_forum_path, class: 'list-group-item' %>
  </div>
</div>
<div class="col-xs-7">
  <ol class="breadcrumb">
    <li><%= link_to (t :home_page), root_path %></li>
    <li><%= link_to (t :members_area), new_member_registration_path %></li>
    <li class="active">加入我們</li>
  </ol>
  <div class="content">
    <div class="page-header">
      <h2>加入我們</h2>
    </div>
    <p>填寫表單，當即註冊成為普通會員，獲取最新資訊。</p>
    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: 'form-horizontal', role: 'form'}, wrapper: :horizontal_form) do |f| %>
      <%= f.error_notification %>
      <%= f.input :email, required: true, autofocus: true, html: { class: 'form-control'} %>
      <%= f.input :password, required: true, hint: ("#{@minimum_password_length} characters minimum" if @validatable) %>
      <%= f.input :password_confirmation, required: true %>
      <div class="form-group">
        <div class="col-xs-offset-4 col-xs-6">
          <%= f.button :submit, "Sign up" %>
        </div>
      </div>
    <% end %>

    <p>或下載相應表格，跟隨指引註冊成為正式會員。</p>
    <div class="row">
      <div class="col-xs-offset-2 col-xs-3">
        <a href="/download/individual.doc" type="button" class="btn btn-danger">個人入會登記表</a>
      </div>
      <div class="col-xs-offset-1 col-xs-3">
        <a href="/download/party.doc" type="button" class="btn btn-danger">團體（單位）入會登記表</a>
      </div>
    </div>
  </div>
</div>
<%= render 'layouts/sidebar' %>