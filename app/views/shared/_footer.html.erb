<div class="footer-content text-center">
  <% if index_page? %>
    <p>
      <% @global_info[:contact_infos].each do |c| %>
        <%= "#{cc c.name}: #{cc c.content}" %>
      <% end %>
    </p>
  <% end %>
  <p>Copyright © 2014 <%= t :msia_title %></p>
</div>